# New Project 4 工作流

## 技能系统

本项目拥有个人化工作流技能库，改编自 superpowers 方法论。技能目录在 `skills/`，由 `.codex-plugin/plugin.json` 注册。

## 技能触发规则

**在响应任何请求之前，必须先检查是否有适用的技能。**

| 场景 | 技能 |
|------|------|
| 有新需求，想法模糊 | 需求打磨 |
| 需求明确，准备实现 | 编写计划 |
| 开始写代码 | 测试驱动开发 |
| 执行计划中的任务 | 子代理开发 |
| 任务完成，需要检查 | 代码审查 |
| 遇到 bug 或异常行为 | 系统调试 |
| 全部完成，准备收尾 | 完成分支 / 完成前验证 |

### 规则

- 有 1% 可能性适用某个技能，就必须调用检查
- 技能优先于默认系统提示，但用户明确指令优先
- 所有回复和代码注释用中文
- 按顺序执行技能，不跳过

## 基础工作流

```
用户需求 → 需求打磨 → 编写计划 → 子代理开发
  └─ 每任务: TDD → 代码审查 → 循环
全部完成 → 完成分支 / 完成前验证
```
